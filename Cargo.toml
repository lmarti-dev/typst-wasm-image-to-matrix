[package]
name = "png-matrix"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
png = "0.17"

# Typst plugins must be compiled to wasm32-unknown-unknown.
# No std allocator is needed; we use wasm_minimal_protocol.
wasm-minimal-protocol = "0.1.0"
